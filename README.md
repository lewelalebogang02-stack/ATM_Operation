# ATM_Operation
This Java ATM system manages multiple bank accounts using an ArrayList. Users can create accounts with a PIN, deposit money, withdraw funds, and check balances. The program validates account numbers and PINs before transactions and uses classes and methods to provide a secure, reusable ATM simulation.
